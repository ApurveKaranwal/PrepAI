{
  "name": "interview-platform-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "backend": "cd backend && python -m uvicorn main:app --host 0.0.0.0 --port 8001 --reload",
    "install:frontend": "npm install --prefix frontend",
    "install:backend": "pip install -r backend/requirements.txt"
  }
}
